const PREVENT_EVENT = "event_prevent_default";

const TEAM_NONE = 0;
const TEAM_SPECTATOR = 1;
const TEAM_CT = 2;
const TEAM_T = 3;

const HUD_PRINT_TALK = 3
const HUD_PRINT_CONSOLE = 2
const HUD_PRINT_CENTER = 4

const GAME_MESSAGE_TARGET_CONSOLE = 'console'
const GAME_MESSAGE_TARGET_HUD = 'hud'

module.exports = {
    PREVENT_EVENT,

    TEAM_NONE,
    TEAM_SPECTATOR,
    TEAM_CT,
    TEAM_T,

    HUD_PRINT_TALK,
    HUD_PRINT_CONSOLE,
    HUD_PRINT_CENTER
}